<%@ page language="java" contentType="text/html; charset=UTF-8" pageEncoding="UTF-8" %>
<%@ include file="includes/header.jsp" %>
<h3>Siparişlerim</h3><table class="table bg-white"><tr><th>No</th><th>Tarih</th><th>Toplam</th><th>Durum</th><th></th></tr><c:forEach var="o" items="${orders}"><tr><td>${o.id}</td><td><fmt:formatDate value="${o.orderDate}" pattern="dd.MM.yyyy HH:mm"/></td><td><fmt:formatNumber value="${o.totalAmount}" type="currency" currencySymbol="₺"/></td><td><c:choose><c:when test="${o.status=='Beklemede'}"><span class="badge bg-warning text-dark">${o.status}</span></c:when><c:when test="${o.status=='Tamamlandı'}"><span class="badge bg-success">${o.status}</span></c:when><c:when test="${o.status=='İptal Edildi'}"><span class="badge bg-danger">${o.status}</span></c:when><c:otherwise><span class="badge bg-info text-dark">${o.status}</span></c:otherwise></c:choose></td><td><a class="btn btn-sm btn-outline-primary" href="${pageContext.request.contextPath}/order-detail?id=${o.id}">Detay</a></td></tr></c:forEach></table>
<%@ include file="includes/footer.jsp" %>
